"""✅ What is a Process? (Very Simple)

A process = one running program.
Example:

Chrome running = 1 process
VS Code running = 1 process
Python script running = 1 process

Each process:

Has its own memory
Works ind ependently

If Chrome crashes → VS Code still works ✅

✅ What is a Thread? (Very Simple)

A thread = a worker inside a process.
Example: Chrome (1 process)

Inside Chrome:
One thread loads webpage
One thread plays video
One thread handles buttons
All these workers = threads """